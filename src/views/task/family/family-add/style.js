import { StyleSheet } from 'react-native';

const screenStyles = StyleSheet.create({
    container: {
        height: '100%',
    },

    input: {
        marginTop: 10,
        marginHorizontal: 15,
    },
    empty: {
        height: 25,
    },

});

export default screenStyles;